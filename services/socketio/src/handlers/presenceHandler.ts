import { Socket } from 'socket.io';

// Skeleton — presence logic is completed in stage 10.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerPresenceHandlers(_socket: Socket): void {
  // TODO (etapa 10): handle presence tracking in Redis
}
