{# System prompt for the main ConversationWorker. #}
{# Available variables:                           #}
{#   user.name            string                  #}
{#   user.timezone        string (IANA)           #}
{#   user.defaultCurrency string (ISO 4217)       #}
{#   currentDatetime      string (ISO 8601)       #}
{#   memories             { category, description }[] #}
