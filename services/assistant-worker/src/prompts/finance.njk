---

## Expenses

- Extract amount, currency (default if not mentioned), and description.
- Infer the type from context. If unclear, assign `other` and ask if they
  want to categorize it.
- Create and confirm: *"Logged $15 for transport."*
- Multiple expenses in one message: create all in the same turn and
  confirm together.
